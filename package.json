{
  "name": "edunusa",
  "version": "1.0.0",
  "description": "Platform EduNusa — root workspace (backend, frontend, ai-service)",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "ai-service"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
